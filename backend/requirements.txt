
# ==============================================================================
# Voice EMR — Python Dependencies
# ==============================================================================
# Install:  pip install -r requirements.txt
# Note: PyTorch must be installed BEFORE pyannote.audio to avoid conflicts.
# ==============================================================================


# ── Core Numerical ────────────────────────────────────────────────────────────
numpy==1.26.4
scipy>=1.10.0,<1.13.0


# ── PyTorch Stack ─────────────────────────────────────────────────────────────
# For CPU-only install (recommended unless you have NVIDIA GPU):
#   pip install torch==2.1.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cpu
# For CUDA 11.8:
#   pip install torch==2.1.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cu118
torch==2.1.2
torchaudio==2.1.2


# ── Audio Processing ──────────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.10.1
noisereduce>=3.0.0      # AI-powered noise reduction for clinical environments


# ── Transcription (Whisper) ───────────────────────────────────────────────────
faster-whisper>=1.0.3
sentencepiece>=0.1.99,<0.2.0
ai4bharat-transliteration==0.5.0.2

# Optional local Indic ASR backend:
# AI4Bharat IndicConformer models currently require the AI4Bharat NeMo fork.
# Install that manually when using TRANSCRIPTION_BACKEND=indicconformer.
# Example:
#   git clone https://github.com/AI4Bharat/NeMo.git
#   cd NeMo
#   git checkout nemo-v2
#   bash reinstall.sh


# ── Speaker Diarization (PyAnnote) ───────────────────────────────────────────
pyannote.audio==3.1.1
huggingface_hub>=0.19.0,<0.23.0
lightning>=2.1.0,<2.3.0
torchmetrics>=1.2.0,<1.4.0


# ── Backend API ───────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
websockets>=12.0        # WebSocket support for live recording endpoint
httpx>=0.27.0           # Async HTTP client (used by FastAPI test client)


# ── Database ──────────────────────────────────────────────────────────────────
mysql-connector-python>=8.2.0
# ibm-db>=3.2.0         # Optional — IBM DB2 for hospital production deploy


# ── Security / Encryption ─────────────────────────────────────────────────────
pycryptodome>=3.20.0


# ── Config & Utilities ────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pandas>=2.0.3          # Required by pyannote.database at runtime
requests>=2.31.0       # Required by huggingface_hub/faster-whisper stack
tqdm>=4.66.0           # Required by faster-whisper progress/runtime utilities


# ── Hardware Detection ────────────────────────────────────────────────────────
psutil>=5.9.0           # System resource monitoring (CPU, RAM)
GPUtil>=1.4.0           # NVIDIA GPU detection


